{
  "name": "energeia-cloud-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Energeia Cyprus All-Energy Network",
  "main": "lib/newsAggregator.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "private": true
}
